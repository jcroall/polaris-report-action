export declare function githubCreateCheck(checkName: string, githubToken: string): Promise<GitHubCheck>;
export declare class GitHubCheck {
    checkName: string;
    checkRunId: number;
    githubToken: string;
    constructor(checkName: string, checkRunId: number, githubToken: string);
    passCheck(summary: string, text: string): Promise<void>;
    failCheck(summary: string, text: string): Promise<void>;
    skipCheck(): Promise<void>;
    cancelCheck(): Promise<void>;
    private finishCheck;
}

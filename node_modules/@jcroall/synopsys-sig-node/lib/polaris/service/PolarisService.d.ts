import PolarisConnection from "../model/PolarisConnection";
export default class PolarisService {
    log: any;
    polaris_url: string;
    access_token: string;
    bearer_token: string | null;
    headers: any | null;
    axios: any;
    constructor(log: any, connection: PolarisConnection);
    authenticate(): Promise<void>;
    get_job(job_status_url: string): Promise<any>;
    fetch_bearer_token(): Promise<string>;
    fetch_cli_modified_date(url: string): Promise<any>;
    fetch_issue_data(url: string): Promise<any>;
    get_url(url: string): Promise<any>;
    fetch_organization_name(): Promise<string | null>;
    download_cli(url: string, file: string): Promise<unknown>;
}

import PolarisService from "./PolarisService";
export default class PolarisJobService {
    log: any;
    polaris_client: PolarisService;
    constructor(log: any, polaris_client: PolarisService);
    waitForJobsToEnd(status_job_urls: string[]): Promise<void>;
    waitForJobToEnd(status_job_url: string): Promise<void>;
    hasJobEnded(status_job_url: string): Promise<boolean>;
    checkJobSuccess(status_job_url: string): Promise<boolean>;
}

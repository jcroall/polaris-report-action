import { Logger } from "winston";
export default class ChangeSetFileWriter {
    log: Logger;
    constructor(log: Logger);
    write_change_set_file(file: string, paths: Array<string>): Promise<number>;
}
